# API Key for Google Gemini (Required for Gemini pipeline/labeling)
GEMINI_API_KEY=your_gemini_api_key_here

# (Optional) Ollama Host configuration if running Ollama remotely or on a custom port
# Defaults to http://localhost:11434 (127.0.0.1:11434)
# OLLAMA_HOST=127.0.0.1:11434
